[workspace]
authors = ["clash-native <329424465+clash-native@users.noreply.github.com>"]
channels = ["conda-forge"]
name = "clash-native"
platforms = ["win-64"]
version = "0.1.0"

[tasks]
format = "python scripts/format.py"
format-check = "python scripts/format.py --check"

[dependencies]
clang-format = ">=22,<23"
cmake = ">=3.24,<5"
ninja = ">=1.12,<2"
